# Storing SDF Objects

Use object:import to export objects from the development account. 

Do *not* use a subdirectory structure because the standard SDF import process does not support it when we are automating work.

